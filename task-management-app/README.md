# React + Vite

When clone the repo then used below command to load the dependencies 
npm install 

After that use below command to start the app 
npm run dev


For better performance and faster development experience, I chose to use React with Vite.

